/**
 * API Configuration Utility
 * Frontend-only mode - NO backend calls
 */

export function getApiBaseUrl(): string {
  // Frontend-only mode: return empty to disable backend calls
  // All features work locally without any backend
  return '';
}
